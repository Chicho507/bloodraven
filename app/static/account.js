"use strict";
document.querySelector("#password-form").addEventListener("submit",async event=>{event.preventDefault();const form=event.currentTarget,error=document.querySelector("#password-error"),button=document.querySelector("#password-save");error.hidden=true;if(form.elements.new.value!==form.elements.repeat.value){error.textContent="Las contraseñas nuevas no coinciden.";error.hidden=false;return;}button.disabled=true;try{await BloodRaven.json("/api/auth/password",{method:"POST",body:JSON.stringify({current:form.elements.current.value,new:form.elements.new.value})});form.reset();location.replace("/login");}catch(failure){error.textContent=failure.message;error.hidden=false;}finally{button.disabled=false;}});
